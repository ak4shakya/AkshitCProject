#include<stdio.h>
int main()
{
    int num_user, rev_num, d1, d2, d3, d4, d5;
    printf("Enter a 5 digit Number:");
   /* scanf("%d", &num_user);
    d5 = num_user%10;
    num_user = num_user/10;
    d4 = num_user%10;
    num_user = num_user/10;
    d3 = num_user%10;
    num_user = num_user/10;
    d2 = num_user%10;
    num_user = num_user/10;
    d1 = num_user%10;
    rev_num = d5*10000 + d4*1000 + d3*100 + d2*10 + d1;
    printf("The Reversed Number is:%d\n", rev_num);
    if(num_user == rev_num)
    {
        printf("The number is equal to its reverse");
    }
    else    
    {
        printf("The number is not equal to its reverse\n");
    }*/
    return 0;
}
        